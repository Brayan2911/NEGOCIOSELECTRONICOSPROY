```html
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Strydr — Cada paso cuenta</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Work+Sans:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root{
    --bg: #14161B;
    --bg-alt: #1D2027;
    --chalk: #F4F2EC;
    --chalk-dim: #A9ADB8;
    --lane: #2B2F38;
    --cobalt: #3B62FF;
    --track-red: #FF4D3D;
    --radius: 4px;
  }

  *{ box-sizing:border-box; margin:0; padding:0; }

  html{ scroll-behavior:smooth; }

  body{
    background:var(--bg);
    color:var(--chalk);
    font-family:'Work Sans', sans-serif;
    line-height:1.5;
    overflow-x:hidden;
  }

  .display{ font-family:'Bebas Neue', sans-serif; letter-spacing:0.02em; }
  .mono{ font-family:'IBM Plex Mono', monospace; }

  a{ color:inherit; text-decoration:none; }

  .lanes{
    position:fixed;
    inset:0;
    z-index:0;
    background-image: repeating-linear-gradient(
      100deg,
      transparent 0 140px,
      var(--lane) 140px 143px
    );
    opacity:0.5;
    pointer-events:none;
  }

  header{
    position:sticky; top:0; z-index:10;
    display:flex; align-items:center; justify-content:space-between;
    padding:22px 6vw;
    background:rgba(20,22,27,0.85);
    backdrop-filter: blur(8px);
    border-bottom:1px solid var(--lane);
  }
  .logo{
    font-size:1.9rem;
    display:flex; align-items:baseline; gap:6px;
  }
  .logo span{ color:var(--cobalt); }

  nav ul{
    display:flex; gap:2.4rem;
    list-style:none;
    font-size:0.95rem;
    font-weight:500;
    color:var(--chalk-dim);
  }
  nav ul li a:hover{ color:var(--chalk); }

  .nav-cta{
    padding:10px 20px;
    border:1px solid var(--chalk-dim);
    border-radius:999px;
    font-size:0.9rem;
    font-weight:600;
    transition: border-color .2s, color .2s;
  }
  .nav-cta:hover{ border-color:var(--chalk); }

  .navbar-inner{ display:flex; align-items:center; gap:2.6rem; }

  @media (max-width: 820px){
    nav ul{ display:none; }
  }

  .hero{
    position:relative; z-index:1;
    padding: 9vw 6vw 7vw;
    display:grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap:3rem;
    align-items:end;
    min-height: 78vh;
  }

  .hero-eyebrow{
    font-family:'IBM Plex Mono', monospace;
    color:var(--cobalt);
    font-size:0.85rem;
    letter-spacing:0.14em;
    text-transform:uppercase;
    margin-bottom:1.4rem;
    display:flex; align-items:center; gap:10px;
  }
  .hero-eyebrow::before{
    content:"";
    width:26px; height:2px;
    background:var(--cobalt);
    display:inline-block;
  }

  .hero h1{
    font-size: clamp(3.6rem, 9vw, 8rem);
    line-height:0.92;
    text-transform:uppercase;
  }
  .hero h1 em{
    font-style:normal;
    color:transparent;
    -webkit-text-stroke: 2px var(--chalk);
  }

  .hero-sub{
    margin-top:1.6rem;
    max-width:32ch;
    color:var(--chalk-dim);
    font-size:1.05rem;
  }

  .hero-actions{
    margin-top:2.4rem;
    display:flex; gap:1rem; flex-wrap:wrap;
  }

  .btn{
    display:inline-flex; align-items:center; gap:10px;
    padding:16px 28px;
    border-radius:999px;
    font-weight:600;
    font-size:0.95rem;
    cursor:pointer;
    border:none;
  }
  .btn-primary{
    background:var(--chalk);
    color:var(--bg);
    transition: transform .15s ease;
  }
  .btn-primary:hover{ transform: translateY(-2px); }
  .btn-ghost{
    background:transparent;
    border:1px solid var(--lane);
    color:var(--chalk);
  }
  .btn-ghost:hover{ border-color:var(--chalk-dim); }

  .stride-panel{
    position:relative;
    border:1px solid var(--lane);
    border-radius: var(--radius);
    padding:2rem;
    background:var(--bg-alt);
    min-height:280px;
    display:flex;
    flex-direction:column;
    justify-content:space-between;
  }
  .stride-panel .km{
    font-family:'IBM Plex Mono', monospace;
    color:var(--chalk-dim);
    font-size:0.8rem;
    letter-spacing:0.1em;
  }
  .stride-track{
    position:relative;
    height:60px;
    margin: 1.4rem 0;
  }
  .stride-track svg{ width:100%; height:100%; }
  .stride-stat{
    display:flex; justify-content:space-between; align-items:baseline;
  }
  .stride-stat .big{ font-family:'Bebas Neue'; font-size:2.6rem; }
  .stride-stat .small{ font-family:'IBM Plex Mono'; font-size:0.75rem; color:var(--chalk-dim); }

  .marquee{
    position:relative; z-index:1;
    border-top:1px solid var(--lane);
    border-bottom:1px solid var(--lane);
    padding:1.1rem 0;
    overflow:hidden;
    white-space:nowrap;
    background:var(--bg-alt);
  }
  .marquee-track{
    display:inline-block;
    animation: scroll 22s linear infinite;
    font-family:'Bebas Neue';
    font-size:1.3rem;
    color:var(--chalk-dim);
    letter-spacing:0.05em;
  }
  .marquee-track span{ margin:0 1.6rem; }
  .marquee-track span::after{ content:"—"; margin-left:1.6rem; color:var(--cobalt); }
  @keyframes scroll{
    from{ transform:translateX(0); }
    to{ transform:translateX(-50%); }
  }

  .section{
    position:relative; z-index:1;
    padding: 6.5rem 6vw;
  }
  .section-head{
    display:flex; justify-content:space-between; align-items:flex-end;
    margin-bottom:2.6rem;
    gap:2rem;
    flex-wrap:wrap;
  }
  .section-head .tag{
    font-family:'IBM Plex Mono'; font-size:0.8rem; color:var(--cobalt);
    letter-spacing:0.1em; text-transform:uppercase; margin-bottom:0.6rem; display:block;
  }
  .section-head h2{
    font-family:'Bebas Neue'; font-size:clamp(2.2rem, 4.5vw, 3.4rem); text-transform:uppercase;
  }
  .section-head p{ color:var(--chalk-dim); max-width:34ch; }

  .cat-grid{
    display:grid;
    grid-template-columns: repeat(3, 1fr);
    gap:1px;
    background:var(--lane);
    border:1px solid var(--lane);
  }
  .cat-card{
    background:var(--bg);
    padding:2.6rem 2rem;
    min-height:280px;
    display:flex; flex-direction:column; justify-content:space-between;
    transition: background .2s;
  }
  .cat-card:hover{ background:var(--bg-alt); }
  .cat-num{ font-family:'IBM Plex Mono'; color:var(--chalk-dim); font-size:0.85rem; }
  .cat-card h3{ font-family:'Bebas Neue'; font-size:2.2rem; text-transform:uppercase; margin-top:1.4rem; }
  .cat-card p{ color:var(--chalk-dim); font-size:0.9rem; margin-top:0.6rem; }
  .cat-link{ margin-top:1.4rem; font-size:0.85rem; font-weight:600; display:inline-flex; align-items:center; gap:6px; }
  .cat-link .arrow{ transition: transform .2s; }
  .cat-card:hover .cat-link .arrow{ transform: translateX(4px); }

  @media (max-width: 820px){
    .cat-grid{ grid-template-columns:1fr; }
    .hero{ grid-template-columns:1fr; }
  }

  .values{
    display:grid;
    grid-template-columns: repeat(3, 1fr);
    gap:2.5rem;
    border-top:1px solid var(--lane);
    padding-top:3rem;
  }
  .value-item .num{ font-family:'IBM Plex Mono'; color:var(--cobalt); font-size:0.8rem; }
  .value-item h4{ font-family:'Bebas Neue'; font-size:1.5rem; text-transform:uppercase; margin:0.6rem 0 0.4rem; }
  .value-item p{ color:var(--chalk-dim); font-size:0.9rem; }
  @media (max-width: 820px){ .values{ grid-template-columns:1fr; } }

  footer{
    position:relative; z-index:1;
    padding: 5rem 6vw 3rem;
    border-top:1px solid var(--lane);
  }
  .footer-top{
    display:flex; justify-content:space-between; align-items:flex-end; flex-wrap:wrap; gap:2rem;
    margin-bottom:3rem;
  }
  .footer-top h2{
    font-family:'Bebas Neue'; font-size:clamp(2.4rem,6vw,4.4rem); text-transform:uppercase; max-width:14ch;
  }
  .footer-cols{
    display:flex; gap:4rem; flex-wrap:wrap;
    color:var(--chalk-dim); font-size:0.9rem;
  }
  .footer-cols strong{ color:var(--chalk); display:block; margin-bottom:0.6rem; font-family:'IBM Plex Mono'; font-size:0.75rem; letter-spacing:0.08em; text-transform:uppercase; }
  .footer-bottom{
    margin-top:3rem; padding-top:1.6rem; border-top:1px solid var(--lane);
    display:flex; justify-content:space-between; color:var(--chalk-dim); font-size:0.8rem; flex-wrap:wrap; gap:1rem;
  }
</style>
</head>
<body>

<div class="lanes"></div>

<header>
  <div class="logo display"><span>Str</span>ydr</div>
  <div class="navbar-inner">
    <nav>
      <ul>
        <li><a href="#catalogo">Catálogo</a></li>
        <li><a href="#nosotros">Nosotros</a></li>
        <li><a href="#contacto">Contacto</a></li>
      </ul>
    </nav>
    <a href="#login" class="nav-cta">Iniciar sesión</a>
  </div>
</header>

<section class="hero">
  <div>
    <div class="hero-eyebrow">Calzado deportivo — colección 2026</div>
    <h1 class="display">Cada<br>paso<br><em>cuenta.</em></h1>
    <p class="hero-sub">Running, básquetbol y estilo urbano seleccionados para acompañarte en cada meta que te propongas.</p>
    <div class="hero-actions">
      <a href="#catalogo" class="btn btn-primary">Ver productos</a>
      <a href="#nosotros" class="btn btn-ghost">Conoce Strydr</a>
    </div>
  </div>

  <div class="stride-panel">
    <div class="km">STRYDR / TRACKING</div>
    <div class="stride-track">
      <svg viewBox="0 0 300 60" fill="none">
        <line x1="0" y1="30" x2="300" y2="30" stroke="#2B2F38" stroke-width="1"/>
        <circle cx="20" cy="30" r="4" fill="#3B62FF"/>
        <circle cx="90" cy="30" r="4" fill="#3B62FF"/>
        <circle cx="160" cy="30" r="4" fill="#3B62FF"/>
        <circle cx="230" cy="30" r="4" fill="#F4F2EC"/>
        <circle cx="280" cy="30" r="6" fill="#FF4D3D"/>
      </svg>
    </div>
    <div class="stride-stat">
      <div>
        <div class="big display">120+</div>
        <div class="small">MODELOS DISPONIBLES</div>
      </div>
      <div>
        <div class="big display">3</div>
        <div class="small">CATEGORÍAS</div>
      </div>
    </div>
  </div>
</section>

<div class="marquee">
  <div class="marquee-track">
    <span>RUNNING</span><span>BÁSQUETBOL</span><span>URBANO</span><span>EDICIONES LIMITADAS</span>
    <span>RUNNING</span><span>BÁSQUETBOL</span><span>URBANO</span><span>EDICIONES LIMITADAS</span>
  </div>
</div>

<section class="section" id="catalogo">
  <div class="section-head">
    <div>
      <span class="tag">Catálogo</span>
      <h2>Elige tu terreno</h2>
    </div>
    <p>Tres categorías, un mismo objetivo: que cada zancada se sienta segura.</p>
  </div>
  <div class="cat-grid">
    <div class="cat-card">
      <span class="cat-num mono">01</span>
      <div>
        <h3>Running</h3>
        <p>Amortiguación ligera para asfalto y pista.</p>
        <span class="cat-link">Ver colección <span class="arrow">→</span></span>
      </div>
    </div>
    <div class="cat-card">
      <span class="cat-num mono">02</span>
      <div>
        <h3>Básquetbol</h3>
        <p>Agarre y soporte de tobillo para la cancha.</p>
        <span class="cat-link">Ver colección <span class="arrow">→</span></span>
      </div>
    </div>
    <div class="cat-card">
      <span class="cat-num mono">03</span>
      <div>
        <h3>Urbano</h3>
        <p>Estilo diario sin sacrificar comodidad.</p>
        <span class="cat-link">Ver colección <span class="arrow">→</span></span>
      </div>
    </div>
  </div>
</section>

<section class="section" id="nosotros" style="padding-top:0;">
  <div class="values">
    <div class="value-item">
      <span class="num mono">— 01</span>
      <h4>Curaduría real</h4>
      <p>Seleccionamos cada modelo pensando en cómo se usa, no solo en cómo se ve.</p>
    </div>
    <div class="value-item">
      <span class="num mono">— 02</span>
      <h4>Para cada atleta</h4>
      <p>De la pista al asfalto, tenemos algo para tu tipo de pisada y disciplina.</p>
    </div>
    <div class="value-item">
      <span class="num mono">— 03</span>
      <h4>Envíos rastreables</h4>
      <p>Sigue tu pedido paso a paso, igual que sigues tu progreso.</p>
    </div>
  </div>
</section>

<footer id="contacto">
  <div class="footer-top">
    <h2 class="display">¿Listo para dar el siguiente paso?</h2>
    <a href="#catalogo" class="btn btn-primary">Ver productos</a>
  </div>
  <div class="footer-cols">
    <div>
      <strong>Strydr</strong>
      Aguascalientes, Ags.<br>Lun–Sáb 10:00–20:00
    </div>
    <div>
      <strong>Navegación</strong>
      Catálogo<br>Nosotros<br>Contacto
    </div>
    <div>
      <strong>Síguenos</strong>
      Instagram<br>TikTok<br>Facebook
    </div>
  </div>
  <div class="footer-bottom">
    <span>© 2026 Strydr. Proyecto escolar simulado.</span>
    <span class="mono">SIN BACKEND — SOLO NAVEGACIÓN</span>
  </div>
</footer>

</body>
</html>
```